version: '3.8'

services:
  web:
    image: php:8.2-apache
    container_name: simple-web
    ports:
      - "8080:80"
    volumes:
      - ./:/var/www/html
    restart: unless-stopped
    environment:
      - APACHE_SERVER_NAME=localhost